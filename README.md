

from root directory
g++ -o dist/spacemaybe src/*.cpp

creates dist/spacemaybe

for cmake:
sudo apt update && sudo apt install cmake

sfml
sudo apt install libsfml-dev
in root directory container src/
cmake -B build
cmake --build build

to make in build dir
make

create build/spacemaybe


